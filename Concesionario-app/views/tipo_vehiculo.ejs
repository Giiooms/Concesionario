<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tipos de Vehículo</title>
  <!-- Enlace a Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    h1 {
      margin-top: 20px;
    }
    .btn-nuevo {
      margin-bottom: 20px;
    }
    .table-actions a {
      margin-right: 10px;
    }
  </style>
</head>
<body>
  <div class="container mt-4">
    <h1 class="text-center">Tipos de Vehículo</h1>
    <div class="text-end">
      <a href="/tipo_vehiculo/nuevo" class="btn btn-primary btn-nuevo">Nuevo Tipo de Vehículo</a>
    </div>
    <table class="table table-striped table-bordered">
      <thead class="table-dark">
        <tr>
          <th>ID</th>
          <th>Tipo</th>
          <th>Acciones</th>
        </tr>
      </thead>
      <tbody>
        <% tipos.forEach(tipo => { %>
          <tr>
            <td><%= tipo.id_tipo %></td>
            <td><%= tipo.tipo %></td>
            <td class="table-actions">
              <a href="/tipo_vehiculo/editar/<%= tipo.id_tipo %>" class="btn btn-warning btn-sm">Editar</a>
              <a href="/tipo_vehiculo/eliminar/<%= tipo.id_tipo %>" class="btn btn-danger btn-sm">Eliminar</a>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  </div>
  <!-- Enlace a Bootstrap JavaScript -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>