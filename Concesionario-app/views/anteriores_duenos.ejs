<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Antiguos Dueños</title>
  <!-- Enlace a Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    h1 {
      margin-top: 20px;
    }
    .btn-nuevo {
      margin-bottom: 20px;
    }
    .table-actions a {
      margin-right: 5px;
    }
  </style>
</head>
<body>
  <div class="container mt-4">
    <h1 class="text-center">Gestión de Antiguos Dueños</h1>
    <div class="text-end">
      <a href="/anteriores_duenos/nuevo" class="btn btn-primary btn-nuevo">Nuevo Antiguo Dueño</a>
    </div>
    <table class="table table-striped table-bordered">
      <thead class="table-dark">
        <tr>
          <th>ID</th>
          <th>Vehículo</th>
          <th>Nombre</th>
          <th>Acciones</th>
        </tr>
      </thead>
      <tbody>
        <% duenos.forEach(dueno => { %>
          <tr>
            <td><%= dueno.id_dueno %></td>
            <td>
              <!-- Mostrar nombre del vehículo o su ID como fallback -->
              <%= dueno.nombre_vehiculo || dueno.id_vehiculo %>
            </td>
            <td><%= dueno.nombre %></td>
            <td class="table-actions">
              <a href="/anteriores_duenos/editar/<%= dueno.id_dueno %>" class="btn btn-warning btn-sm">Editar</a>
              <a href="/anteriores_duenos/eliminar/<%= dueno.id_dueno %>" class="btn btn-danger btn-sm">Eliminar</a>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
  <!-- Enlace a Bootstrap JavaScript -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
